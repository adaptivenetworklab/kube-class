kind: Pod
apiVersion: v1
metadata:
  namespace: dev
  name: apple-app
  labels:
    app: apple
spec:
  containers:
    - name: apple-app
      image: hashicorp/http-echo
      args:
        - "-text=apple"

---

kind: Service
apiVersion: v1
metadata:
  namespace: dev
  name: apple-service
spec:
  selector:
    app: apple
  ports:
    - port: 5678 # Default port for image