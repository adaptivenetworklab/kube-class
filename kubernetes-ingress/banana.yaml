kind: Pod
apiVersion: v1
metadata:
  name: banana-app
  namespace: dev
  labels:
    app: banana
spec:
  containers:
    - name: banana-app
      image: hashicorp/http-echo
      args:
        - "-text=banana"

---

kind: Service
apiVersion: v1
metadata:
  namespace: dev
  name: banana-service
spec:
  selector:
    app: banana
  ports:
    - port: 5678 # Default port for image